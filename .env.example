# Server Configuration
PORT=5001

# Database (SQLite - filename is set in knexfile.ts)
# DB_CLIENT=sqlite3
# DB_CONNECTION_FILENAME=./database.sqlite

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_here # Replace with a strong, random key
JWT_EXPIRES_IN=1d # Example: 1 day
# For production, use a long, randomly generated string for JWT_SECRET

# Add other environment variables as needed
# E.g., API keys for external services, etc.
